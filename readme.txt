Building an Application Using Classes from the course [Object-oriented Programming in JavaScript - ES6](https://app.pluralsight.com/library/courses/javascript-es6-object-oriented-programming/table-of-contents).
  On a Mac, you may need to specify sudo before each command depending on how your permissions are set up.

You need to install node with npm.

This will read package.json and install the needed packages.

Then execute: npm run dev

That will start up the development web server and browser sync.